# @natalia/browser-bridge

External browser bridge for controlling the user's existing browser.

- Chromium extension (Chrome / Edge / Brave / Opera / Vivaldi / Arc)
- Local HTTP/WebSocket server
- Exposes `browser_*` capabilities to Natalia runtime tools
