/**
 * The session package's BROWSER entry (the vite config aliases
 * `@anthelia/session` here for the web build): the node-free surface.
 * The node entry — this plus the two stores — stays at ./index.
 */
export * from "./facts";
