import type {
  InteractiveWaiter,
  InteractiveWaiterDeps,
} from "@anthelia/runtime-services";
export type { InteractiveWaiter, InteractiveWaiterDeps };
import type {
  ApprovalResponse,
  InteractiveResponseOutcome,
  QuestionResponse,
  RuntimeEvent,
  SessionID,
} from "@anthelia/contracts";
import type { ProviderToolCall } from "@anthelia/runtime";
import type { RuntimeTool } from "@anthelia/tools";

/** Interactive waiter contracts, moved from runtime-services with the token. */
