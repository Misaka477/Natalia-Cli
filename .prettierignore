packages/plugins/native-terminal/wezterm/

# Generated by the reference writer (`API_REFERENCE_WRITE=1 bun run
# packages/hosts/transport/test/api-reference.test.ts`), which emits its own
# layout and checks its output appears verbatim in these files. Formatting them
# here would make `docs:check` fail, so the two gates would fight; the generated
# block is the generator's to lay out. The hand-written sections of these files
# are not covered either, which is the cost of that — the alternative is teaching
# the generator to emit prettier output, and that is a change to make when
# something needs it, not in advance.
docs/api-reference.md
docs/api-reference.zh-CN.md
docs/types-reference.md
docs/types-reference.zh-CN.md
docs/config-reference.md
docs/config-reference.zh-CN.md
